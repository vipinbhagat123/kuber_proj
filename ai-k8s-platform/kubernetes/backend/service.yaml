apiVersion: v1
kind: Service

metadata:
  name: ai-backend-service
  namespace: ai-platform

spec:
  selector:
    app: ai-backend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: ClusterIP